# Theme configuration (merged into the site config).
# Register woff2 so resources.GetRemote can resolve the media type of the
# self-hosted webfonts fetched in layouts/_partials/fonts.html.
[mediaTypes."font/woff2"]
suffixes = ["woff2"]
